{
    "$id": "https://schemas.3d.si.edu/voyager/item.schema.json",
    "$schema": "http://json-schema.org/draft-07/schema#",

    "title": "3D Item",
    "description": "Describes a Smithsonian DPO 3D repository item.",

    "definitions": {
        "info": {
            "$id": "#info",
            "type": "object",
            "properties": {
                "type": {
                    "type": "string"
                },
                "copyright": {
                    "type": "string"
                },
                "generator": {
                    "type": "string"
                },
                "version": {
                    "type": "string"
                }
            },
            "required": [
                "type",
                "version"
            ],
            "additionalProperties": false
        }
    },

    "type": "object",
    "properties": {
        "info": {
            "description": "Information about the type, generator, and version of this data structure.",
            "$ref": "#info"
        },
        "meta": {
            "description": "Meta data about this item, including title, record info, collection, etc.",
            "$ref": "meta.schema.json"
        },
        "process": {
            "description": "Information about how this item was digitized and processed.",
            "$ref": "process.schema.json"
        },
        "model": {
            "description": "Describes the visual representations (models, derivatives).",
            "$ref": "model.schema.json"
        },
        "articles": {
            "description": "References to external articles (documents, media files) containing additional information.",
            "$ref": "articles.schema.json"
        },
        "annotations": {
            "description": "Spatial annotations (hot spots, hot zones). Annotations can reference articles.",
            "$ref": "annotations.schema.json"
        }
    },
    "required": [
        "info"
    ],
    "additionalProperties": false
}